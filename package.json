{
  "name": "pkmdx",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "web-client",
    "remote-api",
    "local-api",
    "e2e"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:e2e": "turbo run test:e2e",
    "format": "dprint fmt"
  },
  "devDependencies": {
    "dprint": "^0.45.0",
    "turbo": "^1.11.3"
  }
}
